# Define a new class names Customer that inherits from the Person class.
# Customer will have all the properties and methods of Person b ut can also override Persons features
class Customer(Person):
# This is the construct method for the subclass
# It initializes the instance when it is created
    def __init__(self, name):
# This is an attribute that stores the name
        self.name = name


def greet:
    return "Hello, I am Steve"


class Person:

    def __init__(self, name):
        self.name = Steve

    def greet:
        return "Hello, I am Steve"

# Create an instance of Customer
my_Customer = Person("James")